# 3880.T 更新版評価レポート（DAIO PAPER CORP）（2026-03-15）

このレポートは、`3880.T` に対して実施した trailing 条件付き 27 run 深掘りの結果をまとめたものです。
目的は、現時点での暫定ベスト条件と、今後の位置づけを明確にすることです。

---

## 1. 検証概要

今回の `3880.T` 検証では、次の条件を固定したうえで 27 run を実施した。

- `BREAK_EVEN_R=0.5`
- `TRAILING_START_R=0.5`
- `TRAILING_STOP_R=0.25`
- `TAKE_PROFIT_RR=2.0`
- `VOL_SPIKE_M=1.0`
- `RSI_MIN=30`
- `RSI_MAX=80`
- `USE_INTRADAY_RESOLUTION=1`

変化させた項目:
- `RISK_PCT=0.001,0.0015,0.002`
- `MAX_HOLD_DAYS=3,5,7`
- `MACD_ATR_K=0.11,0.13,0.15`

---

## 2. 現時点の最良条件

現時点で最も良かった run は次の条件だった。

- ticker: `3880.T`
- period: `2024-01-01` から `2026-03-15`
- `RISK_PCT=0.001`
- `MACD_ATR_K=0.11`
- `MAX_HOLD_DAYS=3`
- `BREAK_EVEN_R=0.5`
- `TRAILING_START_R=0.5`
- `TRAILING_STOP_R=0.25`

対応する run:
- run_id: `17308`
- total_return: `3.2343%`
- sharpe: `1.0963`
- max_drawdown: `0.6876%`
- n_trades: `15`

---

## 3. 分かったこと

### 3.1 有効だった傾向
- `MACD_ATR_K=0.11` が最良
- `0.13` と `0.15` は平均成績が同じで、今回の範囲では上積みが見えない
- trailing 条件により一定の改善は見える

### 3.2 効きが弱かった項目
- `RISK_PCT=0.001,0.0015,0.002` は平均成績が完全に同一
- `MAX_HOLD_DAYS=3,5,7` も平均成績が完全に同一
- つまり今回の範囲では、差はほぼ `MACD_ATR_K` に集約されている

### 3.3 現時点の位置づけ
- プラスではあるが、`4385.T`, `3964.T`, `4275.T` より一段下
- ただし候補群の中では十分に残してよい水準
- 本命ではないが、サブ候補としては悪くない

---

## 4. 現時点の評価

`3880.T` は、trailing 条件との相性は悪くないが、現時点では上位陣ほどの強さは見えていない。
今後さらに深掘りするなら `MACD_ATR_K` の近辺に絞るのが自然で、優先度は `4385.T` より下でよい。

---

## 5. 現時点の暫定採用条件

次回以降の `3880.T` 検証では、まず以下を暫定標準条件とする。

- `RISK_PCT=0.001`
- `MACD_ATR_K=0.11`
- `MAX_HOLD_DAYS=3`
- `BREAK_EVEN_R=0.5`
- `TRAILING_START_R=0.5`
- `TRAILING_STOP_R=0.25`
- `RSI_MIN=30`
- `RSI_MAX=80`
- `VOL_SPIKE_M=1.0`
- `TAKE_PROFIT_RR=2.0`

---

## 6. 次の方針

次に進むなら、以下が自然。

1. `MACD_ATR_K=0.10,0.11,0.12` の 9 run で局所最適化する
2. 優先度は `4385.T` より下に置きつつ、比較対象として残す
3. 先に `4385.T` や `4275.T` の深掘りを優先する
