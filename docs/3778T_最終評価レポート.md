# 3778.T 最終評価レポート（SAKURA INTERNET）（2026-03-10）

このレポートは、2026-03-10 に実施した `3778.T` の一連のバックテスト検証を最終整理したものです。
目的は、現時点での到達点、向いているパラメータ、これ以上深掘りしない判断理由を残すことです。

---

## 1. 検証概要

今回の `3778.T` 検証では、次の3段階を実施した。

1. 54 run の動作確認 sweep
2. `RSI_MIN` を固定した 27 run の follow-up
3. `MACD_ATR_K` と `RISK_PCT` を絞った 9 run の再検証

すべて Supabase の `backtests_runs` / `backtests_trades` に保存確認済み。

---

## 2. 最終的に有力だった条件

現時点で最も良かった run は次の条件だった。

- ticker: `3778.T`
- period: `2024-01-01` から `2026-03-10`
- `RISK_PCT=0.002`
- `MACD_ATR_K=0.12`
- `MAX_HOLD_DAYS=5`
- `RSI_MIN=30`
- `RSI_MAX=80`
- `VOL_SPIKE_M=1.0`
- `TAKE_PROFIT_RR=2.0`
- `GAP_ENTRY_MAX=0.08`
- `EXIT_ON_REVERSE=True`

対応する run:
- run_id: `17054`
- final_equity: `3027291.29`
- total_return: `0.9097%`
- max_drawdown: `0.3578%`
- sharpe: `0.7061`
- n_trades: `6`

---

## 3. 分かったこと

### 3.1 有効だった傾向
- `RISK_PCT` は低い方が安定して良かった
- `VOL_SPIKE_M=1.0` は今回の比較では最も良かった
- `RSI_MIN=30`, `RSI_MAX=80` は少なくとも今回の範囲では悪くなかった

### 3.2 効きが弱かった項目
- `MACD_ATR_K=0.10,0.12,0.15` は今回の範囲では差がほぼ出なかった
- `MAX_HOLD_DAYS=3,5,7` も今回の範囲では差が出なかった
- `TAKE_PROFIT_RR=2.0,3.0,4.0` も結果差がほぼ出なかった

### 3.3 気になる点
- `SELL` 理由はほぼ `SL` に寄っていた
- `TP`, `REV`, `TIME` が十分に出ていない
- `n_trades` は多くても 6 程度で、統計的にはまだ弱い
- 結果のユニーク数が少なく、似た run が多かった

---

## 4. 現時点の評価

`3778.T` は、この実装で「極端に合わない」わけではなく、一定の改善余地も見えた。
ただし、現時点で得られた差分は小さく、深掘りを続けるより、他の有望銘柄へ横展開して比較する方が効率的と判断できる。

つまり、`3778.T` は以下の位置づけとする。

- 完全に不適ではない
- 初期検証対象としては十分に役立った
- 現段階では優先度を一段下げる
- 今後、他銘柄比較のベンチマークとして使う価値はある

---

## 5. 当面の採用条件

次の3銘柄比較では、まず以下を `3778.T` 由来の暫定標準条件として使う。

- `RISK_PCT=0.002`
- `MACD_ATR_K=0.12`
- `MAX_HOLD_DAYS=5`
- `RSI_MIN=30`
- `RSI_MAX=80`
- `VOL_SPIKE_M=1.0`
- `TAKE_PROFIT_RR=2.0`
- `GAP_ENTRY_MAX=0.08`
- `EXIT_ON_REVERSE=True`

---

## 6. 次の方針

次は、比較的この実装が向いている候補として挙がっている以下を同条件で確認する。

- `3103.T`
- `1911.T`
- `8306.T`

ここで `3778.T` より明確に良い銘柄が出るかを見て、優先銘柄を絞り込む。
