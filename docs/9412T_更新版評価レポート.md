# 9412.T 更新版評価レポート（スカパーＪＳＡＴ ＨＬＤＧ）

## 1. 概要

`9412.T（スカパーＪＳＡＴ ＨＬＤＧ）` は、単発比較でも深掘りでも安定して上位に残った。  
利益率は `6752.T（パナソニック ＨＬＤＧ）` や `6363.T（酉島製作所）` に少し届かないが、`sharpe` は十分良い。

## 2. 深掘り結果

- ベスト run: `17969`
- `total_return=4.7559%`
- `sharpe=1.6480`
- `max_drawdown=0.5699%`
- `n_trades=15`

ベスト条件:
- `RISK_PCT=0.001`
- `STOP_PCT=0.001`
- `MAX_HOLD_DAYS=3`
- `MACD_ATR_K=0.11`
- `BREAK_EVEN_R=0.5`
- `TRAILING_START_R=0.5`
- `TRAILING_STOP_R=0.25`

## 3. パラメータ傾向

- `MACD_ATR_K=0.11` が最良
- `0.13`, `0.15` にしても大きく崩れない
- `RISK_PCT` は差が出ない
- `MAX_HOLD_DAYS` も差が出ない

平均比較:
- `MACD_ATR_K=0.11`: `4.7559%`
- `MACD_ATR_K=0.13`: `4.6761%`
- `MACD_ATR_K=0.15`: `4.6322%`

## 4. 期間偏りの見え方

`paper simulation` では四半期分散は比較的良い。

四半期ベース:
- `2025-Q2`: `4件`, `22,983.17`
- `2025-Q3`: `1件`, `3,711.74`
- `2025-Q4`: `4件`, `53,425.89`
- `2026-Q1`: `1件`, `3,087.61`

`2025-Q4` が最も強いが、複数期にまたがって利益が出ており、`6363.T（酉島製作所）` より分散は良い。

## 5. FY only block の影響

`FY only block` を入れた `paper simulation` では影響なしだった。

- base: `83,208.42`
- FY only block: `83,208.42`

現時点では、`FY` 回避ルールとの相性は良い。

## 6. 現時点の評価

`9412.T（スカパーＪＳＡＴ ＨＬＤＧ）` は、新規候補の中では安定感のある次点上位候補。  
総合的には `6752.T（パナソニック ＨＬＤＧ）` と `6363.T（酉島製作所）` に少し譲るが、継続監視対象として十分残せる。
