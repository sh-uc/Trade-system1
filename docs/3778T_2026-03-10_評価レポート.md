# 3778.T バックテスト評価レポート（SAKURA INTERNET）（2026-03-10）

`docs/Codex評価レポート標準フォーマット.md` に沿って作成したレポート。
対象は 2026-03-10 に保存確認した 54 run。

---

## 1. 依頼内容
- 対象銘柄: `3778.T`
- 対象期間: `2024-01-01` から `2026-03-10` まで
- 変更したパラメータ: `MAX_HOLD_DAYS=5`, `VOL_SPIKE_M=1.0,1.1,1.2`, `TAKE_PROFIT_RR=2.0,3.0,4.0`, `RISK_PCT=0.004,0.006,0.008`, `RSI_MAX=80`, `MACD_ATR_K=0.15`
- 固定したパラメータ: `TICKER=3778.T`, `START=2024-01-01`, `CAPITAL=3000000`, `PER_TRADE=500000`, `LOT_SIZE=100`, `STOP_SLIPPAGE=0.0025`, `GAP_ENTRY_MAX=0.08`, `EXIT_ON_REVERSE=True`
- 実行本数: `54`
- 保存先: `backtests_runs`, `backtests_trades`

## 2. 実行結果サマリー
- 実行成功 / 失敗: 成功
- 保存成功 / 失敗: 成功
- run 件数: `54`
- trade 件数: `612`
- 代表 run_id 範囲: `16972 - 17025`
- 備考: 想定 27 本ではなく、現行 `bt_sweep_inproc.py` の既定値により `RSI_MIN=30,35` の2通りも含まれたため 54 本になった

## 3. 上位結果
- ベスト run_id: `16973`
- ベスト final_equity: `3023820.25`
- ベスト total_return: `0.7940%`
- ベスト max_drawdown: `0.3578%`
- ベスト sharpe: `0.6300`
- ベスト n_trades: `6`
- ベスト params: `RISK_PCT=0.004`, `TAKE_PROFIT_RR=2.0`, `MAX_HOLD_DAYS=5`, `VOL_SPIKE_M=1.0`, `MACD_ATR_K=0.15`, `RSI_MIN=30`, `RSI_MAX=80`

## 4. 下位結果
- ワースト run_id: `17010`
- ワースト final_equity: `3000836.74`
- ワースト total_return: `0.0279%`
- ワースト max_drawdown: `0.7197%`
- ワースト sharpe: `0.0252`
- ワースト n_trades: `6`
- ワースト params: `RISK_PCT=0.008`, `TAKE_PROFIT_RR=2.0`, `MAX_HOLD_DAYS=5`, `VOL_SPIKE_M=1.1`, `MACD_ATR_K=0.15`, `RSI_MIN=35`, `RSI_MAX=80`

## 5. 傾向の要約
- 何のパラメータが効いたか: 今回は `VOL_SPIKE_M` と `RISK_PCT` の差が比較的はっきり出た。`VOL_SPIKE_M=1.0` の平均 `total_return` は `0.7105%`、`1.1` は `0.1298%`、`1.2` は `0.2104%`。`RISK_PCT` は `0.004 > 0.006 > 0.008` の順で平均成績が低下した
- ほぼ同じ結果が多いか: `54 run` に対して `final_equity` のユニーク値は `9` で、かなり似た結果が多い
- 取引回数は十分か: `n_trades` は `5` または `6` で、比較には使えるが十分多いとは言いづらい
- 利益とドローダウンのバランス: ベストでもリターン差は小さく、全体平均 `total_return=0.3502%`, 平均 `max_drawdown=0.4831%` なので、優位性はまだ強くない
- SELL reason の特徴: `SELL` 理由は今回すべて `SL` だった。`TP`, `REV`, `TIME` は出ていない

## 6. 問題や注意点
- データ取得上の問題: 今回はなし
- 保存上の問題: 今回はなし。`backtests_runs` と `backtests_trades` ともに保存確認済み
- ロジック上の懸念: `TAKE_PROFIT_RR=2.0,3.0,4.0` で平均成績がまったく同じだったため、利確条件が結果に効いていない可能性がある
- 検証件数の不足: `54 run` は動作確認としては十分だが、最適化判断には少ない
- 次回までに直したい点: `RSI_MIN` を固定して 27 本に揃えること、`TP` が効いていない理由を確認すること

## 7. 次の提案
- 続けて試すべきパラメータ: `VOL_SPIKE_M` は `1.0` 周辺を細かく、`RISK_PCT` は `0.004` より低い側も試す価値がある
- 固定してよさそうなパラメータ: 今回の範囲では `MACD_ATR_K=0.15`, `MAX_HOLD_DAYS=5`, `RSI_MAX=80` はこのまま据え置いてよい
- 除外候補の条件: `VOL_SPIKE_M=1.1` かつ `RISK_PCT=0.008` 付近は優先度を下げてよい
- 次回の実行テーマ: `RSI_MIN` を1値に固定した 27 run で再実行し、`MAX_HOLD_DAYS` と `MACD_ATR_K` の効きを比較する

## 8. 参照メモ
- run_id 範囲: `16972 - 17025`
- 平均 total_return: `0.3502%`
- 平均 max_drawdown: `0.4831%`
- 平均 n_trades: `5.67`
- final_equity のユニーク数: `9`
- n_trades のユニーク数: `2`
