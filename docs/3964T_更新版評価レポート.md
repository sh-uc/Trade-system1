# 3964.T 更新版評価レポート（AUCNET INC）（2026-03-15）

このレポートは、`3964.T` に対して実施した単発比較、27 run 深掘り、その後の exit 改良検証をまとめた更新版です。
目的は、現時点での暫定ベスト条件と、横展開対象としての位置づけを明確にすることです。

---

## 1. 検証概要

今回の `3964.T` 検証では、次の段階を実施した。

1. 候補銘柄バッチ内での単発比較
2. `RISK_PCT`, `MAX_HOLD_DAYS`, `MACD_ATR_K` の 27 run 深掘り
3. `BREAK_EVEN_R` の導入
4. `TRAILING_START_R`, `TRAILING_STOP_R` の導入と小グリッド比較

すべて Supabase の `backtests_runs` / `backtests_trades` に保存確認済み。

---

## 2. 現時点の最良条件

現時点で最も良かった run は次の条件だった。

- ticker: `3964.T`
- period: `2024-01-01` から `2026-03-15`
- `RISK_PCT=0.001`
- `STOP_PCT=0.001`
- `MACD_ATR_K=0.10`
- `MAX_HOLD_DAYS=3`
- `RSI_MIN=30`
- `RSI_MAX=80`
- `VOL_SPIKE_M=1.0`
- `TAKE_PROFIT_RR=2.0`
- `BREAK_EVEN_R=0.5`
- `TRAILING_START_R=0.5`
- `TRAILING_STOP_R=0.25`
- `GAP_ENTRY_MAX=0.08`
- `EXIT_ON_REVERSE=True`

対応する run:
- run_id: `17236`
- final_equity: `3158331.75`
- total_return: `5.2777%`
- max_drawdown: `0.6344%`
- sharpe: `1.7320`
- n_trades: `25`

近い成績の run:
- `17238` (`TRAILING_START_R=1.0`, total_return `5.2743%`)
- `17246` (`TRAILING_START_R=1.5`, total_return `5.2743%`)

---

## 3. 分かったこと

### 3.1 有効だった傾向
- `MACD_ATR_K=0.10` が引き続き有力
- `BREAK_EVEN_R=0.5` の導入で少し改善した
- `TRAILING_STOP_R=0.25` の導入で `total_return`, `sharpe`, `max_drawdown` がそろって改善した
- `TP` と `SL` のバランスが比較的良く、exit 改良の効果が見えやすい

### 3.2 効きが弱かった項目
- `TRAILING_START_R=0.5, 1.0, 1.5` の差はかなり小さい
- `MAX_HOLD_DAYS` は引き続き効きが弱い
- `RISK_PCT` は差があるものの、exit 改良の影響の方が大きくなってきた

### 3.3 現時点の位置づけ
- `3103.T` ほどではないが、かなり有望
- `BREAK_EVEN + trailing` への反応が素直
- ロジック改善の評価対象として引き続き使いやすい

---

## 4. 現時点の評価

`3964.T` は、候補群の中でもこの実装に十分向いている。
特に、`BREAK_EVEN + trailing` で成績がきれいに伸びており、exit 改良の効果確認に使いやすい。

位置づけとしては次の通り。

- 優先銘柄候補の上位
- `3103.T` に次ぐ改良確認銘柄
- 横展開時のセカンド基準銘柄

---

## 5. 現時点の暫定採用条件

次回以降の `3964.T` 検証では、まず以下を暫定標準条件とする。

- `RISK_PCT=0.001`
- `STOP_PCT=0.001`
- `MACD_ATR_K=0.10`
- `MAX_HOLD_DAYS=3`
- `RSI_MIN=30`
- `RSI_MAX=80`
- `VOL_SPIKE_M=1.0`
- `TAKE_PROFIT_RR=2.0`
- `BREAK_EVEN_R=0.5`
- `TRAILING_START_R=0.5`
- `TRAILING_STOP_R=0.25`
- `GAP_ENTRY_MAX=0.08`
- `EXIT_ON_REVERSE=True`

---

## 6. 次の方針

次に進むなら、以下が自然。

1. この条件を他銘柄に横展開して、候補群の再ランキングを行う
2. `3964.T` を基準に、`MACD_ATR_K=0.09,0.10,0.11` の再微調整を行う
3. `TRAILING_STOP_R` を中心に exit をさらに磨く

現時点では、1 を先にやる価値が高い。
