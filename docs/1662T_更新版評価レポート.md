# 1662.T 更新版評価レポート（JAPAN PETROLEUM EXPLORATION CO）（2026-03-23）

このレポートは、`1662.T` に対して実施した単発比較、27 run 深掘り、`FY only block` の確認結果をまとめたものです。

---

## 1. 現時点の最良条件

現時点で最も良かった run は次の条件だった。

- ticker: `1662.T`
- period: `2024-01-01` から `2026-03-23`
- `RISK_PCT=0.001`
- `STOP_PCT=0.001`
- `MACD_ATR_K=0.11`
- `MAX_HOLD_DAYS=5`
- `RSI_MIN=30`
- `RSI_MAX=80`
- `VOL_SPIKE_M=1.0`
- `TAKE_PROFIT_RR=2.0`
- `BREAK_EVEN_R=0.5`
- `TRAILING_START_R=0.5`
- `TRAILING_STOP_R=0.25`
- `GAP_ENTRY_MAX=0.08`
- `EXIT_ON_REVERSE=True`

対応する run:

- run_id: `17905`
- total_return: `4.8509%`
- sharpe: `1.3056`
- max_drawdown: `0.5228%`
- n_trades: `21`

---

## 2. 分かったこと

### 2.1 有効だった傾向

- `MACD_ATR_K=0.11` が明確に最良
- `0.13` と `0.15` は一段落ちる
- 取引数は `21` と、新規候補群の中では比較的十分

### 2.2 効きが弱かった項目

- `RISK_PCT=0.001,0.0015,0.002` は差が出ない
- `MAX_HOLD_DAYS=3,5,7` も差が出ない

今回も、差はほぼ `MACD_ATR_K` に集中していた。

### 2.3 exit の実態

`SL` が多く見えるが、その中身は大半が利益確定 stop だった。

- `SL利益`: `18`
- `SL損失`: `2`
- `TP`: `1`

したがって、`1662.T` も
`break-even + trailing` で stop を切り上げて利益を守る
型との相性が良い。

---

## 3. FY only block の影響

`1662.T` だけを含む `paper simulation` 比較では、
`FY only block` の影響は今回ほぼ出なかった。

比較:

- base: `127,264.81`
- FY only block: `127,264.81`

今回の期間では、`FY only block` で止まった `1662.T` の取引は確認されていない。

---

## 4. 現時点の評価

`1662.T` は、新規候補群の中ではかなり良い。
ただし、既存の最上位群

- `3103.T`
- `7013.T`
- `6330.T`
- `4506.T`

を上回るほどではなく、
**第二グループの上位候補**
という位置づけが自然である。

---

## 5. 期間偏りの見え方

`paper simulation` ベースで見ると、`1662.T` は期間偏りがやや強い。

四半期別:

- `2025-Q3`: `6` 件, `12,736.12`
- `2025-Q4`: `5` 件, `14,266.82`
- `2026-Q1`: `5` 件, `100,261.87`

月次でも

- `2026-01`: `77,987.70`

が特に大きく、利益の中心が `2026-Q1` に寄っている。

したがって、現時点では

- 安定分散型というより
- 特定局面で強く伸びた型

として扱うのが妥当である。

---

## 6. 現時点の暫定採用条件

次回以降の `1662.T` 検証では、まず以下を暫定標準条件とする。

- `RISK_PCT=0.001`
- `STOP_PCT=0.001`
- `MACD_ATR_K=0.11`
- `MAX_HOLD_DAYS=5`
- `BREAK_EVEN_R=0.5`
- `TRAILING_START_R=0.5`
- `TRAILING_STOP_R=0.25`
- `RSI_MIN=30`
- `RSI_MAX=80`
- `VOL_SPIKE_M=1.0`
- `TAKE_PROFIT_RR=2.0`
- `GAP_ENTRY_MAX=0.08`
- `EXIT_ON_REVERSE=True`

---

## 7. 次の方針

次に進むなら、以下が自然。

1. `1662.T` を新規候補群の上位候補として維持する
2. `FY only block` はこの銘柄では軽く使えそうなので、その前提で母集団比較に残す
3. さらに候補が増えたときの比較基準銘柄の1つにする
