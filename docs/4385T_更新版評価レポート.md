# 4385.T 更新版評価レポート（MERCARI INC）（2026-03-15）

このレポートは、`4385.T` に対して実施した trailing 条件付き 27 run 深掘りの結果をまとめたものです。
目的は、現時点での暫定ベスト条件と、今後の深掘り優先度を明確にすることです。

---

## 1. 検証概要

今回の `4385.T` 検証では、次の条件を固定したうえで 27 run を実施した。

- `BREAK_EVEN_R=0.5`
- `TRAILING_START_R=0.5`
- `TRAILING_STOP_R=0.25`
- `TAKE_PROFIT_RR=2.0`
- `VOL_SPIKE_M=1.0`
- `RSI_MIN=30`
- `RSI_MAX=80`
- `USE_INTRADAY_RESOLUTION=1`

変化させた項目:
- `RISK_PCT=0.001,0.0015,0.002`
- `MAX_HOLD_DAYS=3,5,7`
- `MACD_ATR_K=0.11,0.13,0.15`

---

## 2. 現時点の最良条件

現時点で最も良かった run は次の条件だった。

- ticker: `4385.T`
- period: `2024-01-01` から `2026-03-15`
- `RISK_PCT=0.001`
- `MACD_ATR_K=0.13`
- `MAX_HOLD_DAYS=3`
- `BREAK_EVEN_R=0.5`
- `TRAILING_START_R=0.5`
- `TRAILING_STOP_R=0.25`

対応する run:
- run_id: `17306`
- total_return: `4.3194%`
- sharpe: `1.4465`
- max_drawdown: `0.4905%`
- n_trades: `15`

---

## 3. 分かったこと

### 3.1 有効だった傾向
- `MACD_ATR_K=0.13` が明確に最良
- `0.11` も悪くないが、`0.13` が一段強い
- `0.15` ははっきり弱い
- trailing 条件との相性は良い

### 3.2 効きが弱かった項目
- `RISK_PCT=0.001,0.0015,0.002` は平均成績が完全に同一
- `MAX_HOLD_DAYS=3,5,7` も平均成績が完全に同一
- つまり今回の範囲では、主な差は `MACD_ATR_K` に集中している

### 3.3 現時点の位置づけ
- 候補群の中ではかなり有望
- `3964.T`, `4275.T` と並ぶ上位候補
- 次の深掘り対象として妥当

---

## 4. 現時点の評価

`4385.T` は、今回の trailing 条件と相性が良く、横展開候補の中ではトップだった。
一方で、`RISK_PCT` と `MAX_HOLD_DAYS` が効かないため、今後は `MACD_ATR_K` と exit 側を中心に詰めるのが自然である。

---

## 5. 現時点の暫定採用条件

次回以降の `4385.T` 検証では、まず以下を暫定標準条件とする。

- `RISK_PCT=0.001`
- `MACD_ATR_K=0.13`
- `MAX_HOLD_DAYS=3`
- `BREAK_EVEN_R=0.5`
- `TRAILING_START_R=0.5`
- `TRAILING_STOP_R=0.25`
- `RSI_MIN=30`
- `RSI_MAX=80`
- `VOL_SPIKE_M=1.0`
- `TAKE_PROFIT_RR=2.0`

---

## 6. 次の方針

次に進むなら、以下が自然。

1. `MACD_ATR_K=0.12,0.13,0.14` の 9 run で局所最適化する
2. trailing 条件を少しだけ微調整する
3. `3964.T` や `4275.T` と並べて、どれを次の本命にするか判断する
