# 3778.T バックテスト評価レポート（SAKURA INTERNET）（2026-03-10 / follow-up 27 runs）

`RSI_MIN` を固定して 27 run に絞り、`MAX_HOLD_DAYS` と `MACD_ATR_K` の効きを確認した follow-up レポート。

---

## 1. 依頼内容
- 対象銘柄: `3778.T`
- 対象期間: `2024-01-01` から `2026-03-10` まで
- 変更したパラメータ: `RISK_PCT=0.003,0.004,0.005`, `MAX_HOLD_DAYS=3,5,7`, `MACD_ATR_K=0.10,0.15,0.20`
- 固定したパラメータ: `RSI_MIN=30`, `RSI_MAX=80`, `VOL_SPIKE_M=1.0`, `TAKE_PROFIT_RR=2.0`, `CAPITAL=3000000`, `PER_TRADE=500000`, `LOT_SIZE=100`, `STOP_SLIPPAGE=0.0025`, `GAP_ENTRY_MAX=0.08`, `EXIT_ON_REVERSE=True`
- 実行本数: `27`
- 保存先: `backtests_runs`, `backtests_trades`

## 2. 実行結果サマリー
- 実行成功 / 失敗: 成功
- 保存成功 / 失敗: 成功
- run 件数: `27`
- trade 件数: `288`
- 代表 run_id 範囲: `17026 - 17052`
- 備考: 今回は意図どおり `27 run` になった

## 3. 上位結果
- ベスト run_id: `17026`
- ベスト final_equity: `3025555.77`
- ベスト total_return: `0.8519%`
- ベスト max_drawdown: `0.3578%`
- ベスト sharpe: `0.6688`
- ベスト n_trades: `6`
- ベスト params: `RISK_PCT=0.003`, `MAX_HOLD_DAYS=5`, `MACD_ATR_K=0.10`, `RSI_MIN=30`, `RSI_MAX=80`, `VOL_SPIKE_M=1.0`, `TAKE_PROFIT_RR=2.0`

## 4. 下位結果
- ワースト run_id: `17046`
- ワースト final_equity: `3011191.60`
- ワースト total_return: `0.3731%`
- ワースト max_drawdown: `0.3771%`
- ワースト sharpe: `0.3797`
- ワースト n_trades: `4`
- ワースト params: `RISK_PCT=0.005`, `MAX_HOLD_DAYS=3`, `MACD_ATR_K=0.20`, `RSI_MIN=30`, `RSI_MAX=80`, `VOL_SPIKE_M=1.0`, `TAKE_PROFIT_RR=2.0`

## 5. 傾向の要約
- 何のパラメータが効いたか: 今回は `MACD_ATR_K` と `RISK_PCT` の差がはっきり出た。`MACD_ATR_K=0.10` と `0.15` の平均 `total_return` はともに `0.7944%`、`0.20` は `0.4190%` まで低下した。`RISK_PCT` は `0.003 > 0.004 > 0.005` の順で平均成績が低下した
- ほぼ同じ結果が多いか: `27 run` に対して `final_equity` のユニーク値は `6`。前回よりは絞れたが、まだ重複が多い
- 取引回数は十分か: `n_trades` は `4` または `6`。`MACD_ATR_K=0.20` のとき平均 `4` まで落ちており、厳しすぎる可能性がある
- 利益とドローダウンのバランス: 全体平均 `total_return=0.6693%`, 平均 `max_drawdown=0.3550%` で、前回 54 run より改善している
- SELL reason の特徴: 今回も `SELL` 理由はすべて `SL` だった。`TP`, `REV`, `TIME` は依然として出ていない

## 6. 問題や注意点
- データ取得上の問題: なし
- 保存上の問題: なし。`backtests_runs` と `backtests_trades` 保存確認済み
- ロジック上の懸念: `MAX_HOLD_DAYS=3,5,7` の平均成績が完全に同一で、今回の範囲では効いていない
- 検証件数の不足: 27 run としては狙いどおりだが、`VOL_SPIKE_M` や `TAKE_PROFIT_RR` を固定しているため、まだ局所確認の段階
- 次回までに直したい点: `TP` が効かない理由と、`MAX_HOLD_DAYS` が効かない理由を分解したい

## 7. 次の提案
- 続けて試すべきパラメータ: `MACD_ATR_K` は `0.10 - 0.15` の間を細かく、`RISK_PCT` は `0.003` より低い側も試す価値がある
- 固定してよさそうなパラメータ: 今回の結果では `RSI_MIN=30`, `RSI_MAX=80`, `VOL_SPIKE_M=1.0`, `TAKE_PROFIT_RR=2.0` は仮固定候補
- 除外候補の条件: `MACD_ATR_K=0.20` は優先度を下げてよい
- 次回の実行テーマ: `MACD_ATR_K=0.10,0.12,0.15` と `RISK_PCT=0.002,0.003,0.004` に絞って再実行し、`TP` と `MAX_HOLD_DAYS` が本当に無効なのか追加確認する

## 8. 参照メモ
- run_id 範囲: `17026 - 17052`
- 平均 total_return: `0.6693%`
- 平均 max_drawdown: `0.3550%`
- 平均 n_trades: `5.33`
- final_equity のユニーク数: `6`
- n_trades のユニーク数: `2`
- `MAX_HOLD_DAYS`: `3,5,7` で差なし
- `MACD_ATR_K`: `0.10/0.15` は同等、`0.20` は悪化
