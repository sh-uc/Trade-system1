# Codex評価レポート標準フォーマット

この文書は、バックテストや sweep 実行後に Codex が返す評価レポートの標準フォーマットです。
毎回同じ観点で比較できるようにすることが目的です。

---

## 1. 依頼内容
- 対象銘柄:
- 対象期間:
- 変更したパラメータ:
- 固定したパラメータ:
- 実行本数:
- 保存先:

## 2. 実行結果サマリー
- 実行成功 / 失敗:
- 保存成功 / 失敗:
- run 件数:
- trade 件数:
- 代表 run_id 範囲:
- 備考:

## 3. 上位結果
- ベスト run_id:
- ベスト final_equity:
- ベスト total_return:
- ベスト max_drawdown:
- ベスト sharpe:
- ベスト n_trades:
- ベスト params:

## 4. 下位結果
- ワースト run_id:
- ワースト final_equity:
- ワースト total_return:
- ワースト max_drawdown:
- ワースト sharpe:
- ワースト n_trades:
- ワースト params:

## 5. 傾向の要約
- 何のパラメータが効いたか:
- ほぼ同じ結果が多いか:
- 取引回数は十分か:
- 利益とドローダウンのバランス:
- SELL reason の特徴:

## 6. 問題や注意点
- データ取得上の問題:
- 保存上の問題:
- ロジック上の懸念:
- 検証件数の不足:
- 次回までに直したい点:

## 7. 次の提案
- 続けて試すべきパラメータ:
- 固定してよさそうなパラメータ:
- 除外候補の条件:
- 次回の実行テーマ:

## 8. Codex 記入例
- 実行成功。`3778.T` を 2024-01-01 から 54 本流し、`backtests_runs` 54 件、`backtests_trades` 612 件を確認。
- 上位は `VOL_SPIKE_M=1.0〜1.2`, `TAKE_PROFIT_RR=2.0〜4.0`, `RISK_PCT=0.004〜0.008` の範囲で大差は小さかった。
- 今回は `RSI_MIN` が 2 通り入っていたため、想定 27 本ではなく 54 本になった。
- 次回は `RSI_MIN` を固定して本数を絞り、`MAX_HOLD_DAYS` と `MACD_ATR_K` の効きを見たい。
