# 6752.T 更新版評価レポート（パナソニック ＨＬＤＧ）

## 1. 概要

`6752.T（パナソニック ＨＬＤＧ）` は、新規候補群の中ではかなり有力だった。  
単発比較でも上位に入り、27 run 深掘りでも `total_return` と `sharpe` のバランスが良い。

## 2. 深掘り結果

- ベスト run: `17996`
- `total_return=5.0484%`
- `sharpe=1.9247`
- `max_drawdown=0.4396%`
- `n_trades=19`

ベスト条件:
- `RISK_PCT=0.001`
- `STOP_PCT=0.001`
- `MAX_HOLD_DAYS=3`
- `MACD_ATR_K=0.11`
- `BREAK_EVEN_R=0.5`
- `TRAILING_START_R=0.5`
- `TRAILING_STOP_R=0.25`

## 3. パラメータ傾向

- `MACD_ATR_K=0.11` が最良
- `0.13` と `0.15` は同水準で、一段弱い
- `RISK_PCT` は差が出ない
- `MAX_HOLD_DAYS` も差が出ない

平均比較:
- `MACD_ATR_K=0.11`: `5.0484%`
- `MACD_ATR_K=0.13`: `4.6909%`
- `MACD_ATR_K=0.15`: `4.6909%`

## 4. 期間偏りの見え方

`paper simulation` では極端な一発偏重ではなく、比較的分散して利益が出ている。

四半期ベース:
- `2025-Q3`: `2件`, `3,344.78`
- `2025-Q4`: `6件`, `66,821.28`
- `2026-Q1`: `4件`, `40,008.90`

完全な均等ではないが、`2025-Q4` と `2026-Q1` の両方で利益が出ており、局面分散は悪くない。

## 5. FY only block の影響

`FY only block` を入れた `paper simulation` では影響なしだった。

- base: `110,174.96`
- FY only block: `110,174.96`

現時点では、`FY` 回避ルールと両立しやすい銘柄と見てよい。

## 6. 現時点の評価

`6752.T（パナソニック ＨＬＤＧ）` は、新規候補の中ではかなり上位。  
既存有力群の本命4銘柄を脅かすほどではないが、次点上位に入れる価値がある。
