# 6330.T 更新版評価レポート（TOYO ENGINEERING CORP）（2026-03-17）

このレポートは、`6330.T` に対して実施した trailing 条件付き 27 run 深掘りの結果をまとめたものです。
目的は、現時点での暫定ベスト条件と、候補群内での位置づけを明確にすることです。

---

## 1. 検証概要

今回の `6330.T` 検証では、次の条件を固定したうえで 27 run を実施した。

- `BREAK_EVEN_R=0.5`
- `TRAILING_START_R=0.5`
- `TRAILING_STOP_R=0.25`
- `TAKE_PROFIT_RR=2.0`
- `VOL_SPIKE_M=1.0`
- `RSI_MIN=30`
- `RSI_MAX=80`
- `USE_INTRADAY_RESOLUTION=1`

変化させた項目:
- `RISK_PCT=0.001,0.0015,0.002`
- `MAX_HOLD_DAYS=3,5,7`
- `MACD_ATR_K=0.11,0.13,0.15`

---

## 2. 現時点の最良条件

現時点で最も良かった run は次の条件だった。

- ticker: `6330.T`
- period: `2024-01-01` から `2026-03-17`
- `RISK_PCT=0.001`
- `MAX_HOLD_DAYS=3`
- `MACD_ATR_K=0.15`
- `BREAK_EVEN_R=0.5`
- `TRAILING_START_R=0.5`
- `TRAILING_STOP_R=0.25`

対応する run:
- run_id: `17720`
- total_return: `9.2041%`
- sharpe: `1.9028`
- max_drawdown: `1.0216%`
- n_trades: `15`

---

## 3. 分かったこと

### 3.1 有効だった傾向
- `MACD_ATR_K=0.15` が最良
- `0.11` もかなり近い
- リターンはかなり強い

### 3.2 効きが弱かった項目
- `RISK_PCT=0.001,0.0015,0.002` は差が出ない
- `MAX_HOLD_DAYS=3,5,7` も差が出ない
- 差はほぼ `MACD_ATR_K` に集約されている

### 3.3 現時点の位置づけ
- リターン面ではかなり有望
- `4506.T` より `sharpe` はやや下だが、十分に上位候補
- `7013.T`, `4506.T` に次ぐ有力候補として扱える

---

## 4. 現時点の評価

`6330.T` は、今回の trailing 条件とかなり相性が良く、リターンの強さが目立つ。
今後の候補群の中では、引き続き深掘り対象として残す価値が高い。

---

## 5. 現時点の暫定採用条件

次回以降の `6330.T` 検証では、まず以下を暫定標準条件とする。

- `RISK_PCT=0.001`
- `MAX_HOLD_DAYS=3`
- `MACD_ATR_K=0.15`
- `BREAK_EVEN_R=0.5`
- `TRAILING_START_R=0.5`
- `TRAILING_STOP_R=0.25`
- `RSI_MIN=30`
- `RSI_MAX=80`
- `VOL_SPIKE_M=1.0`
- `TAKE_PROFIT_RR=2.0`

---

## 6. 次の方針

次に進むなら、以下が自然。

1. `MACD_ATR_K=0.14,0.15,0.16` の 9 run で局所最適化する
2. `4506.T` と並べて、本命候補の優先順位を確認する
3. 更新版評価レポートの対象として固定する
