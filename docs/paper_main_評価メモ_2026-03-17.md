# paper_main 評価メモ（2026-03-17）

このメモは、`paper_main` を `2025-01-01` 開始で実行した結果を、
実運用寄り検証の観点で短く整理したものです。

---

## 1. 実行条件

- simulation: `paper_main`
- strategy: `swing_v1`
- 期間: `2025-01-01` から実行時点まで
- 対象銘柄:
  - `3103.T`
  - `7013.T`
  - `4506.T`
  - `6235.T`
  - `6330.T`
- active な `strategy_param_sets` を使用
- `lot_size`, 建玉管理, 約定履歴を含む `paper simulation` 最小版で実行

---

## 2. 全体結果

- `live_positions`: `63` 件
- `live_trade_fills`: `126` 件
- 実行終了時の `open_positions`: `0`
- 実行終了時の `cash`: `4,185,318`
- 初期資金 `3,000,000` に対して、全体としてはプラスで終了

決済理由の全体内訳:
- `SL`: `58`
- `TP`: `5`

このため、`paper simulation` でも現状のロジックは
`TP` で大きく伸ばすより、`SL` を多く含みながら全体で積み上げる形になっている。

---

## 3. 銘柄別の見え方

| ticker | positions | realized_pnl | TP | SL |
|---|---:|---:|---:|---:|
| `3103.T` | 13 | 421,449.91 | 1 | 12 |
| `6330.T` | 12 | 288,379.04 | 3 | 9 |
| `6235.T` | 11 | 185,657.89 | 1 | 10 |
| `4506.T` | 13 | 167,666.73 | 0 | 13 |
| `7013.T` | 14 | 122,164.53 | 0 | 14 |

---

## 4. 実運用寄り検証でも強い銘柄

### 4.1 最上位

- `3103.T`
  - `paper simulation` でも最も大きい損益を確保
  - これまでの `backtest` の強さが、実運用寄り検証でも崩れていない

### 4.2 次点

- `6330.T`
  - `TP` が 3 件あり、今回の5銘柄の中では一番 `TP` が出ている
  - 実運用寄りでも利益額が大きく、有力候補としてかなり強い

### 4.3 安定してプラス圏

- `6235.T`
- `4506.T`
- `7013.T`

この3銘柄もプラスで終わっており、候補から外す必要はない。
ただし今回の範囲では、`3103.T` と `6330.T` が一段上に見える。

---

## 5. 現時点の整理

`backtest` と `paper simulation` の両方を見ると、現時点の実運用寄り有力銘柄は次の並びが自然。

1. `3103.T`
2. `6330.T`
3. `6235.T`
4. `4506.T`
5. `7013.T`

補足:
- `7013.T` は `backtest` では非常に強かったが、今回の `paper simulation` では利益額は控えめだった
- `4506.T` は `sharpe` の良さが魅力だが、実運用寄りではまだ様子見余地がある
- `6330.T` は今回、`backtest` の強さを比較的そのまま持ち込めている

---

## 6. 次に見るべきこと

- `paper_main` を別期間でも回して、順位が安定するか確認する
- `7013.T` が `paper simulation` で伸びきらない理由を確認する
- 決算回避やイベント停止を入れたときに順位がどう変わるかを見る

今回の結果だけで銘柄の最終順位を断定はしないが、
少なくとも `3103.T` は `backtest` だけでなく `paper simulation` でも本命候補を維持している。

---

## 7. 決算回避ルールの影響（2026-03-22 追記）

公式IRベースで整備した `earnings_expected` を使い、
`EarningsForceCloseDays=1` を固定したうえで
`EarningsBlockDays=2,3,5` を比較した。

比較条件:

- simulation:
  - `paper_main_expected_b2`
  - `paper_main_expected_b3`
  - `paper_main_expected_b5`
- 開始日: `2025-01-01`
- 対象銘柄:
  - `3103.T`
  - `7013.T`
  - `4506.T`
  - `6235.T`
  - `6330.T`
- イベントソース: `ticker_events.event_type='earnings_expected'`

全体結果:

| simulation | positions | realized_pnl | 主な変化 |
|---|---:|---:|---|
| `paper_main` | 63 | 1,185,318.10 | ベース |
| `paper_main_expected_b2` | 61 | 999,128.33 | 決算回避あり |
| `paper_main_expected_b3` | 61 | 999,128.33 | `b2` と同一 |
| `paper_main_expected_b5` | 60 | 984,703.58 | さらに 1 件減少 |

見えたこと:

- `EarningsBlockDays=2` と `3` は今回のデータでは同一結果
- `5` に広げると、`6235.T` の `2025-08-04` open の取引が追加で止まり、さらに利益が低下
- 決算回避は、少なくとも今回の5銘柄セットでは
  「安全側になるが、利益をかなり削る」動きになった

銘柄別の影響:

| ticker | base pnl | b2/b3 pnl | b5 pnl |
|---|---:|---:|---:|
| `3103.T` | 421,449.91 | 351,727.40 | 351,727.40 |
| `4506.T` | 167,666.73 | 167,666.73 | 167,666.73 |
| `6235.T` | 185,657.89 | 89,535.39 | 75,110.64 |
| `6330.T` | 288,379.04 | 288,379.04 | 288,379.04 |
| `7013.T` | 122,164.53 | 101,819.77 | 101,819.77 |

主に止まった取引:

- `3103.T` `2026-02-04` open: `+69,722.51`
- `6235.T` `2026-02-13` open: `+96,122.50`
- `7013.T` `2025-08-06` open: `+18,625.13`
- `6235.T` `2025-08-04` open: `+14,424.75` (`b5` のみ)

このため、現時点の暫定見立ては次のとおり。

- 決算回避ルール自体は必要
- ただし `EarningsBlockDays=5` は止めすぎ
- まず試すなら `2` または `3`
- 特に `6235.T` はイベント回避の影響を強く受けやすい
