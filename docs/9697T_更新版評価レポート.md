# 9697.T 更新版評価レポート（CAPCOM CO LTD）（2026-03-23）

このレポートは、`9697.T` に対して実施した単発比較、27 run 深掘り、`FY only block` の確認結果をまとめたものです。

---

## 1. 現時点の最良条件

現時点で最も良かった run は次の条件だった。

- ticker: `9697.T`
- period: `2024-01-01` から `2026-03-23`
- `RISK_PCT=0.001`
- `STOP_PCT=0.001`
- `MACD_ATR_K=0.11`
- `MAX_HOLD_DAYS=5`
- `RSI_MIN=30`
- `RSI_MAX=80`
- `VOL_SPIKE_M=1.0`
- `TAKE_PROFIT_RR=2.0`
- `BREAK_EVEN_R=0.5`
- `TRAILING_START_R=0.5`
- `TRAILING_STOP_R=0.25`
- `GAP_ENTRY_MAX=0.08`
- `EXIT_ON_REVERSE=True`

対応する run:

- run_id: `17877`
- total_return: `3.5566%`
- sharpe: `1.5184`
- max_drawdown: `0.7353%`
- n_trades: `17`

---

## 2. 分かったこと

### 2.1 有効だった傾向

- `MACD_ATR_K=0.11` が最良
- `0.13` は次点
- `0.15` は明確に弱い

### 2.2 効きが弱かった項目

- `RISK_PCT=0.001,0.0015,0.002` は差が出ない
- `MAX_HOLD_DAYS=3,5,7` も差が出ない

### 2.3 exit の実態

`9697.T` も `SL` が多いが、
その大半は利益確定 stop だった。

- `SL利益`: `14`
- `SL損失`: `1`
- `TP`: `2`

したがって、`9697.T` も
`TP` より `break-even + trailing` による stop 利益確定型
として見るのが正しい。

---

## 3. FY only block の影響

`9697.T` と `1662.T` を含む `paper simulation` 比較では、
`FY only block` の影響はごく小さかった。

比較:

- base: `21,264.11`
- FY only block: `21,209.98`

止まった取引:

- `2025-05-12` open の取引 `+54.13`

今回の範囲では、
`FY only block` はこの銘柄に対してかなり穏やかな安全策として機能している。

---

## 4. 現時点の評価

`9697.T` は、新規候補群の中では十分良いが、
現時点では

- `1662.T`
- `3103.T`
- `7013.T`
- `6330.T`
- `4506.T`

より一段下で、
**新規候補群の準上位**
という位置づけが自然である。

---

## 5. 期間偏りの見え方

`paper simulation` ベースでは、`9697.T` の取引はかなり限定された期間に集中していた。

四半期別:

- `2025-Q2`: `5` 件, `21,264.11`

月次では

- `2025-05`: `2` 件, `6,501.36`
- `2025-06`: `3` 件, `14,762.75`

のみで、今回確認できた利益期間はかなり狭い。

このため、現時点では

- 方向性は悪くないが
- 期間分散の観点ではまだ弱い

と見ておくのが自然である。

---

## 6. 現時点の暫定採用条件

次回以降の `9697.T` 検証では、まず以下を暫定標準条件とする。

- `RISK_PCT=0.001`
- `STOP_PCT=0.001`
- `MACD_ATR_K=0.11`
- `MAX_HOLD_DAYS=5`
- `BREAK_EVEN_R=0.5`
- `TRAILING_START_R=0.5`
- `TRAILING_STOP_R=0.25`
- `RSI_MIN=30`
- `RSI_MAX=80`
- `VOL_SPIKE_M=1.0`
- `TAKE_PROFIT_RR=2.0`
- `GAP_ENTRY_MAX=0.08`
- `EXIT_ON_REVERSE=True`

---

## 7. 次の方針

次に進むなら、以下が自然。

1. `9697.T` を新規候補群の比較対象として残す
2. `FY only block` はこの銘柄では問題なく使えそうなので、その前提で比較を続ける
3. さらなる新規候補と並べて、第二グループ内での順位を確認する
