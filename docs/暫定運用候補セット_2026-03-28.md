# 暫定運用候補セット（2026-03-28）

## 1. 位置づけ

この文書は、現時点で `paper simulation` の結果が比較的良く、  
今後の実運用寄り検証の母集団として使う **暫定運用候補セット** を固定したものです。

対応する `strategy_name`:
- `swing_v1_candidate9`

## 2. 採用銘柄

### 中核4銘柄

- `3103.T` ユニチカ
- `7013.T` IHI
- `4506.T` 住友ファーマ
- `6330.T` 東洋エンジニアリング

### 拡張5銘柄

- `6752.T` パナソニック ホールディングス
- `6363.T` 酉島製作所
- `6366.T` 千代田化工建設
- `3964.T` オークネット
- `9412.T` スカパーJSATホールディングス

## 3. 固定方針

- `strategy_param_sets` に `strategy_name='swing_v1_candidate9'` として登録済み
- `param_set_name='candidate_20260328'`
- `effective_from='2026-03-28'`

## 4. 現時点の使い方

このセットは、
- 実運用寄りの `paper simulation`
- 運用制約の追加検証
- ポートフォリオ単位の比較

の基準セットとして扱う。

## 5. 補足

- `3964.T` オークネットは、利益寄与はあるが `FY only block` の影響を受けやすい
- そのため、暫定運用候補セットには含めるが、個別監視対象として扱う
- それ以外の 8 銘柄は、今回確認した範囲では `FY only block` と比較的両立しやすい
